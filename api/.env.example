# Local API configuration. Copy to a private environment file; never commit secrets.
PORT=8787
OPENAI_MODEL=gpt-5-mini

# Required only for authenticated Supabase quota enforcement.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-public-anon-key

# Never put SUPABASE_SERVICE_ROLE_KEY in this API process or the Extension.
